import { Container } from '@material-ui/core';
import React from 'react';
import StorePage from '../../components/widgets/grid-lists/Store';

const Store = () => (
  <Container sx={{ mt: 11 }}>
    <StorePage />
  </Container>
);

export default Store;
